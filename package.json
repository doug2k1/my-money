{
  "name": "my-money",
  "version": "1.0.0",
  "scripts": {
    "prebuild": "npm ci --prefix backend && npm ci --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix backend"
  },
  "engines": {
    "node": "14.x.x"
  }
}
